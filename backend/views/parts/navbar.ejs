<% locals.pageName ||='' ; %>

    <style>
        .navbar-nav .nav-link.active {
            /* background-color: lightblue; */
            color: white;
            font-weight: 900;
            border-radius: 6px;
        }
    </style>
    <div class="container-fluid">
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <a class="navbar-brand" href="/productCMS">密室逃脫後台管理</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link <%= pageName==='pd_list' ? 'active bg-primary' : '' %>" href="/productCMS">商品列表</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= pageName==='wh_list' ? 'active bg-primary' : '' %>" href="/warehousing">庫存列表</a>
                        </li>

                    </ul>

                    <ul class="navbar-nav mb-2 mb-lg-0">
                        <% if(true){ %>
                            <li class="nav-item">
                                <a class="nav-link">
                                    改jwt
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="/logout">登出</a>
                            </li>
                            <% } else { %>
                                <li class="nav-item">
                                    <a class="nav-link" aria-current="page" href="/login">登入</a>
                                </li>
                                <% } %>
                    </ul>

                </div>
            </div>
        </nav>
    </div>